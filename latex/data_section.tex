%% ═══════════════════════════════════════════════════════════════════════════
%% DATA - included via \input{data_section} from main.tex
%% ═══════════════════════════════════════════════════════════════════════════

\clearpage
\section{Data}\label{sec:data}

This chapter chooses the inputs for the regime classifier. We choose a small subset of nine candidate stress indicators for the HMM to use, so that the cross-sectional model can learn the signal generated by the regime as accurately as possible. A four-pass procedure narrows the candidates, shrinking the set on a different criterion at each pass rather than on a single in-sample fit.

The stock-level sample spans January 1990 to November 2024 and includes all ordinary common shares listed on the NYSE, AMEX, or NASDAQ with a price above \$1.\footnote{The \$1 floor follows \citet{Gu2020}; value-weighting limits small-stock influence on the realized portfolio.} Following \citet{Shumway1997}, delisting returns are incorporated to prevent survivorship bias: actual delisting returns are used when available, and $-30\%$ is imputed for performance-related delistings with missing returns.\footnote{Full data sources and detailed variable definitions are provided in Appendix~\ref{app:data_summary}.} The sample is split into a training period (1990--2010) used to estimate all models and a test period (2011--2024, 167 months) used exclusively for out-of-sample evaluation. No model parameters are re-estimated during the test period. Table~\ref{tab:sample_summary} provides an overview.

\input{tables/table_sample_summary}


\subsection{Feature Selection}\label{sec:feature_selection}

The regime signal's ability to capture the shift in momentum's cross-sectional structure depends critically on which stress indicators the HMM observes. The selection problem has two constraints. First, market drawdown (DD) is anchored as a baseline feature: \citet{DanielMoskowitz2016} show that recent market declines empirically drive the momentum crash, making drawdown the most direct observable measure of the conditions in which the mechanism this thesis studies activates. Second, the number of features is capped at four: with 240 training months,\footnote{The calendar 1990--2010 window spans 252 months; the first 12 are reserved as a warm-up for the 12-month momentum lookbacks, leaving 240 effective training months (Jan 1991--Dec 2010).} each regime has sufficient observations to estimate a 4-dimensional mean vector and covariance matrix (10 free covariance parameters per state), and more features risk estimation instability.

\paragraph{Candidate pool}
Nine monthly macrofinancial indicators, all available from 1990 onward, are considered:
\begin{itemize}
    \item \emph{Equity-market stress:} market drawdown (DD), realized volatility (VOL), and log VIX (LVIX).
    \item \emph{Cross-sectional structure:} cross-sectional return dispersion (DISP), relative market participation (REL\_N), advance-decline ratio (ADR), and cross-sectional return skewness (SKEW).
    \item \emph{Credit and rates:} BAA--AAA credit spread (CS) and the 10-year minus 2-year Treasury term spread (TERM).
\end{itemize}
All candidates are observable in real time at the end of each month.

\paragraph{Selection procedure}
The selection proceeds through four passes of increasing rigour. In the first pass (\emph{quality screen}), all DD-inclusive combinations of up to four features are considered. With 8 remaining candidates, this gives $1 + \binom{8}{1} + \binom{8}{2} + \binom{8}{3} = 1 + 8 + 28 + 56 = 93$ combinations (DD alone, DD paired with one other, DD with two others, DD with three others). Each is estimated with a single Gibbs sampler seed, and combinations are eliminated if the HMM fails basic sanity checks. The two states must separate meaningfully, and the model must correctly classify known crises (the dot-com bust, the GFC, and COVID) as panic while keeping non-crisis periods calm.\footnote{Specific thresholds: ESS $> 50$, regime separation significant on at least $\min(2, D)$ features (where $D$ is the size of the candidate combination), and $\pi_t^{\text{filter}}$ classifies the GFC, COVID, and dot-com crashes as panic while remaining low through calm expansions.}

In the second pass (\emph{portfolio ranking}), surviving combinations are evaluated through the full cross-sectional pipeline, testing whether a better regime signal translates into better stock selection. Combinations are ranked by out-of-sample Sharpe ratio at the selection seed budget (10 HMM seeds, 20 XGBoost seeds). The top 10 advance.

In the third pass (\emph{definitive validation}), the surviving finalists are re-evaluated at higher seed counts to confirm that the Pass-2 ranking is not an artefact of low-seed MCMC noise.

In the fourth pass (\emph{stability check}), the finalists are tested for robustness across random seed partitions. A combination that achieves a high Sharpe on one lucky seed combination but collapses on another is unreliable. We run 20 independent experiments with different seed draws and classify combinations as stable ($\sigma < 0.10$), moderate ($0.10 \leq \sigma < 0.15$), or unstable ($\sigma \geq 0.15$).

\paragraph{Selected set}
Pass~4 selects DD, DISP, REL\_N, and CS as the production set. This combination ranks at the top of the candidate pool on Pass-2 Sharpe and is the most consistent across random seed partitions in Pass~4. It also carries at least one feature from each candidate category (equity-market stress, cross-sectional structure, credit and rates), spanning all three dimensions of stress in the candidate pool.

\subsection{HMM Input Features}\label{sec:hmm_features}

DD is the strongest single-feature classifier (Sharpe 0.66 alone vs 0.97 for the full 4-feature set; Table~\ref{tab:hmm_feature_ablation}, Appendix~\ref{app:hmm_feature_tables}), but leave-one-out diagnostics show the four features contribute on different dimensions. DD and REL\_N pull most of the Sharpe weight: removing either drops the full-set Sharpe by 0.46 and 0.54 respectively. DISP and CS are marginal on the Sharpe metric (removal drops it by 0.01 each) but extend the candidate-category footprint (cross-sectional structure and credit-and-rates respectively) and modestly tighten cross-partition $\sigma$ from $0.14$ to $0.11$ (Table~\ref{tab:feature_selection}). Each of the four captures a distinct dimension of market stress:
\begin{enumerate}
    \item \textbf{DD (Drawdown):} The percentage decline of the cumulative market price index from its rolling 12-month peak:
    \begin{equation}
        DD_t = \frac{P_t - \max(P_{t-11}, \dots, P_t)}{\max(P_{t-11}, \dots, P_t)}.
    \end{equation}
    DD directly measures the depth of ongoing market declines, making it the natural anchor for regime identification.

    \item \textbf{DISP (Return Dispersion):} The log cross-sectional standard deviation of individual stock returns:
    \begin{equation}
        DISP_t = \log\!\left(\text{std}_s(r_{s,t})\right).
    \end{equation}
    DISP captures cross-sectional \emph{disagreement} among stocks. During stress, individual stocks move in different directions rather than together, causing dispersion to spike.

    \item \textbf{REL\_N (Relative Market Participation):} The log ratio of the number of actively traded stocks to its trailing 12-month average:
    \begin{equation}
        REL\_N_t = \log\!\left(\frac{N_t}{\frac{1}{12}\sum_{\ell=1}^{12} N_{t-\ell}}\right).
    \end{equation}
    REL\_N turns negative during crises when stocks are delisted, trading is halted, or acquisitions accelerate. It captures structural market damage that the other features do not measure.

    \item \textbf{CS (Credit Spread):} The difference between Moody's BAA and AAA corporate bond yields:
    \begin{equation}
        CS_t = Y_t^{\text{BAA}} - Y_t^{\text{AAA}}.
    \end{equation}
    The credit spread provides an independent signal from the corporate bond market, helping the HMM distinguish genuine stress episodes from equity-specific turbulence.
\end{enumerate}

All four features are winsorized at the 1st and 99th percentiles\footnote{Winsorizing at the 1st and 99th percentiles caps any value below the 1st percentile at that bound and any value above the 99th percentile at that bound, limiting the influence of outliers without discarding the observations.} using training-sample statistics, and standardized to zero mean and unit variance.

The four series co-move around stress episodes. DD and REL\_N drop while DISP and CS spike during the 2000--2002 dot-com bust, the 2007--2009 Global Financial Crisis, the March 2020 COVID shock, and the 2022 Fed-hiking cycle, with the shaded NBER recession bands aligning with the largest of these joint excursions. This co-movement makes a single hidden-state variable viable in the HMM.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{plots/thesis/features_hmm.png}
    \caption{Time series of the four HMM input features (DD, DISP, REL\_N, CS), standardized using training-sample statistics. Shaded regions indicate NBER recession periods.}
    \label{fig:features_hmm}
\end{figure}

Panic regimes are characterized by deep market drawdowns, elevated cross-sectional dispersion in stock returns, reduced market participation, and widened credit spreads. Calm regimes show shallow or no drawdown, narrow return dispersion, broad participation, and tight credit spreads.

\subsection{Regime Estimation Results}\label{sec:hmm_output}

\paragraph{Sampler convergence}
Convergence of each chain is assessed via the effective sample size (ESS) of post-burn-in draws and the Gelman--Rubin $\hat{R}$ statistic across independent chains, with trace plots as a visual cross-check; formal definitions and the conventional thresholds are collected in Appendix~\ref{app:convergence}. On the selected feature set, the Gibbs sampler converges cleanly. All posterior parameters exceed an ESS of 100,\footnote{ESS $> 100$ per parameter is a standard minimum for reliable posterior inference \citep{Gelman2013}; full definition in Appendix~\ref{app:convergence}.} so the post-burn-in draws contain at least 100 effectively independent samples per parameter. Across five independent chains drawn from the seed pool, the maximum Gelman--Rubin $\hat{R}$ over all HMM parameters is $\hat{R} = 1.001$ (Table~\ref{tab:gelman_rubin}, Appendix~\ref{app:gelman_rubin}),\footnote{The Gelman--Rubin $\hat{R}$ compares within- and between-chain variance \citep{GelmanRubin1992}; full definition and thresholds in Appendix~\ref{app:convergence}.} well inside the conventional ($1.1$) and modern ($1.01$) thresholds.

ESS counts how many effectively independent draws each chain produces. A chain that gets stuck in a small region, or one whose draws are heavily autocorrelated, produces a low ESS. An ESS above $100$ on every parameter means the posterior has a sharp peak that the chain can navigate, which only happens if the four features genuinely separate calm and panic months. Flat or multi-modal likelihoods cannot reach that threshold, no matter how long the sampler runs.

The Gelman--Rubin $\hat{R}$ checks whether chains started from different initialisations find the same answer. $\hat{R} \approx 1$ means they do; $\hat{R} > 1$ means they do not. With $K=2$ fixed, the HMM has to produce two clusters. If the data actually had three or more underlying modes, there would be several reasonable ways to compress them into two, and different chains would pick different ones. $\hat{R} = 1.001$ means every chain found the same two-cluster solution, which only happens if the data really is bimodal. Several candidate feature sets were rejected during selection on related convergence grounds, and the clean values here endorse both the four-feature choice and the $K=2$ specification.

Averaging $\pi_t^{\text{filter}}$ across many independent Gibbs chains stabilizes the regime signal: each chain carries its own seed-specific MCMC noise, and averaging cancels that noise across chains. Table~\ref{tab:seed_convergence} (Appendix~\ref{app:seed_convergence}) shows the resulting downstream Sharpe rises with the number of averaged chains and plateaus around 30; the 200-chain production setting sits well inside that plateau, so the regime signal is not seed-limited.

\clearpage
\paragraph{Regime separation and filtered probability}
Table~\ref{tab:hmm_separation} reports the posterior regime means estimated on the 1990--2010 training period. All four features exhibit statistically significant separation between calm and panic (95\% credible intervals excluding zero),\footnote{A $95\%$ credible interval is the Bayesian range covering the parameter with $95\%$ probability; excluding zero means the panic--calm mean difference on each feature is reliably distinct from zero.} confirming that the four-feature emission space resolves cleanly into two distinct clusters.

\input{tables/table_hmm_separation}

Figure~\ref{fig:regime_probabilities} plots the resulting filtered probability $\pi_t^{\text{filter}}$ (constructed as in Section~\ref{sec:trading_signal}) over the full 1990--2024 sample. The signal spikes at the 2000--2002 dot-com bust, the 2007--2009 GFC, the March 2020 COVID shock, and the 2022 Fed-hiking cycle, and stays near zero during the 2003--2007 and 2013--2019 calm windows. The dashed vertical line marks the train/test split, with the post-2011 portion held out for out-of-sample evaluation.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{plots/thesis/regime_probabilities.png}
    \caption{Filtered regime probability $\pi_t^{\text{filter}}$ (1990--2024). The pink area shows the filtered probability path. The dashed vertical line marks the train/test split (January 2011), the dotted horizontal line marks the $0.5$ threshold above which a month is classified as panic, and the grey vertical bands mark NBER recession periods. Major stress episodes (Dot-com, GFC, COVID) are labeled at the top.}
    \label{fig:regime_probabilities}
\end{figure}


\subsection{Cross-Sectional Features}\label{sec:cs_features}

The cross-sectional models rank stocks each month using twelve trailing momentum signals together with the filtered panic probability $\pi_t^{\text{filter}}$. The latter varies only at the monthly market level (every stock in the same month receives the same value), so it functions as a conditioning variable rather than a stock-level predictor.

Twelve trailing momentum signals are computed for each stock, corresponding to lookback horizons of 1 to 12 months:
\begin{equation}
    \text{mom}_{k,t}^s = \prod_{\ell=1}^{k}(1 + r_{t-\ell}^s) - 1, \qquad k = 1, \dots, 12,
\end{equation}
where $r_{t-\ell}^s$ is the adjusted return of stock $s$ in month $t - \ell$. The current month's return is excluded from all lookbacks to avoid mechanical correlation with the forward return being predicted. Including all twelve horizons rather than a single lookback allows the models to learn how the full momentum term structure interacts with the regime signal.

\subsection{International Sample}\label{sec:intl_sample}

For the robustness check in Section~\ref{sec:international}, the same pipeline is applied to UK and Japanese equities from Compustat Global over the same 1990--2024 window. The HMM feature set is re-selected per region using the four-pass procedure described in Section~\ref{sec:feature_selection}, run separately on each market's data, with the candidate pool adapted to use locally available stress indicators. Both regions select \emph{DD+VOL+REL\_N}. Long and short legs are formed from unconditional decile breakpoints across all listed stocks, since no NYSE-equivalent tier exists for either market. Per-region selection details and stability checks are in Appendix~\ref{app:intl_feature_selection}.

With the regime signal and the cross-sectional features in place, the next chapter evaluates the three portfolio-construction methods out-of-sample and decomposes the XGBoost model's monthly selections.
