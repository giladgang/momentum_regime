# Numbers in latex/*.tex that are NOT thesis result claims.
# Format:  <decimal>  # rationale
# Lines starting with # and blank lines are ignored.
# Match is exact-string after .rstrip('0') / collapse — keep these tight.

# ── Significance / probability thresholds ─────────────────────────────
0.01   # significance threshold (***)
0.05   # significance threshold (**)
0.10   # significance threshold (*)
0.025  # two-sided 2.5% percentile
0.975  # two-sided 97.5% percentile
0.5    # decile cut / median / pi threshold
0.9    # 90% persistence prior
0.10   # decile breakpoint
0.90   # decile breakpoint
2.5    # bootstrap percentile
97.5   # bootstrap percentile

# ── XGB / HMM hyperparameters (config, not result) ──────────────────
0.05   # XGB learning rate
0.8    # XGB subsample / colsample
0.001  # 10 bps trading fee
0.999  # log1p clip lower bound

# ── HMM priors / iterations ────────────────────────────────────────
0.01   # HMM kappa_0 prior (already covered above for sig)
9.0    # Dirichlet diag
1.0    # Dirichlet off-diag

# ── Plot / formatting numbers ───────────────────────────────────────
1.05   # 5% headroom on plot ylim
0.35   # alpha on plot lines
0.7    # plot linewidth alpha
0.5    # plot linewidth alpha
1.8    # plot linewidth
2.5    # centroid plot linewidth (overlaps with bootstrap percentile)
0.3    # grid alpha
0.25   # IQR fill alpha

# ── Document layout (margins, figure sizes — not results) ──────────
0.5    # \linewidth fraction (overlaps median)
0.7    # \linewidth fraction
0.9    # \linewidth fraction
1.0    # full width

# ── Common rounding artefacts of long fractional values ─────────────
0.0    # zero baselines
1.0    # unit
