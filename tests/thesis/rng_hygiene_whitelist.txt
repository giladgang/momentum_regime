# Scripts whitelisted from the module-level RNG-seeding check.
# Format:  <relative_path>  # rationale
# One entry per line; #-prefixed lines and blank lines ignored.
#
# Add a script here only after confirming that the module-level seed call
# is genuinely safe (e.g. it's a self-contained CLI script with no other
# importers, and the seed pin is part of the documented experiment).

scripts/main_results_analysis.py    # CLI script, np.random.seed(42) intentional; no other importer (verified 2026-04-29)
scripts/fundamentals_test.py        # CLI script, np.random.seed(42) intentional; no other importer (verified 2026-04-29)
scripts/external_validity.py        # CLI script, np.random.seed(42) seeds shuffled-pi placebo; no other importer (verified 2026-04-29)
scripts/stress_test.py              # CLI script, np.random.seed(42) inside conditional B7 block; no other importer (verified 2026-04-29)
