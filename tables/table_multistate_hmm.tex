\begin{table}[H]
\centering
\small
\begin{tabular}{l r r}
\toprule
$K$ & M2 Sharpe & Notes \\
\midrule
2 (baseline) & 0.97 & Production model (200 seeds) \\
3 & 0.68 & Single seed; intermediate state unstable \\
\bottomrule
\end{tabular}
\caption{Out-of-sample Sharpe ratios for HMMs with $K = 2$ and $K = 3$ states (long-short construction). The $K = 3$ model underperforms the baseline $K = 2$ because the intermediate state lacks persistence and introduces classification instability. With only 241 training months, the data cannot reliably separate more than two regimes for downstream cross-sectional stock selection.}
\label{tab:multistate_hmm}
\end{table}
